#' Mapping of distribution indexes to names
#'
#' Internal function that maps distribution index (like 1, 2) to actual distribution model names.
#' @keywords internal
distribution_mapping <- c("norm", "std")
